# auto-generated by grc.converter

id: divider_f_divider_xx
label: Frequency Divider
category: Frequency Dividers

parameters:
-   id: type
    label: IO Type
    dtype: enum
    options: [float, int, short]
    option_attributes:
        fcn: [ff, ii, ss]
    hide: part
-   id: count
    label: Count
    dtype: int
    default: '2'

inputs:
-   domain: stream
    dtype: ${ type }

outputs:
-   domain: stream
    dtype: ${ type }

templates:
    imports: import divider
    make: divider.f_divider_${type.fcn}(${count})
    callbacks:
    - set_k(${count})

file_format: 1
